# Copy to the host or platform secrets. Never commit real values.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/appointments"
NODE_ENV=production
PORT=3000
HOSTNAME=0.0.0.0

# Docker Compose only: seed the demo dentist account on first start.
# SEED_ON_START=true
