# Required. Prisma generate needs this even when no live database is present.
DATABASE_URL="postgresql://appointments:appointments@localhost:5432/appointments"

# development | production | test
NODE_ENV=development

# Optional. Next.js listen port (docker / next start).
PORT=3000
